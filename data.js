/* AUTO-GENERATED from data.jsx by build-github.cjs. Do not edit; edit the .jsx and re-run `npm run build:github`. */
const ICONS = {
  search: React.createElement(React.Fragment, null, React.createElement("circle", {
    cx: "11",
    cy: "11",
    r: "7"
  }), React.createElement("path", {
    d: "m20 20-3.5-3.5"
  })),
  plus: React.createElement(React.Fragment, null, React.createElement("path", {
    d: "M12 5v14"
  }), React.createElement("path", {
    d: "M5 12h14"
  })),
  bell: React.createElement(React.Fragment, null, React.createElement("path", {
    d: "M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9"
  }), React.createElement("path", {
    d: "M10 21a2 2 0 0 0 4 0"
  })),
  inbox: React.createElement(React.Fragment, null, React.createElement("polyline", {
    points: "22 12 16 12 14 15 10 15 8 12 2 12"
  }), React.createElement("path", {
    d: "M5.5 5h13l3.5 7v7a2 2 0 0 1-2 2h-17a2 2 0 0 1-2-2v-7Z"
  })),
  list: React.createElement(React.Fragment, null, React.createElement("line", {
    x1: "8",
    y1: "6",
    x2: "21",
    y2: "6"
  }), React.createElement("line", {
    x1: "8",
    y1: "12",
    x2: "21",
    y2: "12"
  }), React.createElement("line", {
    x1: "8",
    y1: "18",
    x2: "21",
    y2: "18"
  }), React.createElement("line", {
    x1: "3",
    y1: "6",
    x2: "3.01",
    y2: "6"
  }), React.createElement("line", {
    x1: "3",
    y1: "12",
    x2: "3.01",
    y2: "12"
  }), React.createElement("line", {
    x1: "3",
    y1: "18",
    x2: "3.01",
    y2: "18"
  })),
  board: React.createElement(React.Fragment, null, React.createElement("rect", {
    x: "3",
    y: "3",
    width: "7",
    height: "18",
    rx: "1"
  }), React.createElement("rect", {
    x: "14",
    y: "3",
    width: "7",
    height: "11",
    rx: "1"
  })),
  queue: React.createElement(React.Fragment, null, React.createElement("path", {
    d: "M21 6H3"
  }), React.createElement("path", {
    d: "M21 12H8"
  }), React.createElement("path", {
    d: "M21 18H3"
  })),
  users: React.createElement(React.Fragment, null, React.createElement("path", {
    d: "M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"
  }), React.createElement("circle", {
    cx: "9",
    cy: "7",
    r: "4"
  }), React.createElement("path", {
    d: "M22 21v-2a4 4 0 0 0-3-3.87"
  }), React.createElement("path", {
    d: "M16 3.13a4 4 0 0 1 0 7.75"
  })),
  chart: React.createElement(React.Fragment, null, React.createElement("line", {
    x1: "18",
    y1: "20",
    x2: "18",
    y2: "10"
  }), React.createElement("line", {
    x1: "12",
    y1: "20",
    x2: "12",
    y2: "4"
  }), React.createElement("line", {
    x1: "6",
    y1: "20",
    x2: "6",
    y2: "14"
  })),
  settings: React.createElement(React.Fragment, null, React.createElement("circle", {
    cx: "12",
    cy: "12",
    r: "3"
  }), React.createElement("path", {
    d: "M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 1 1-4 0v-.09a1.65 1.65 0 0 0-1-1.51 1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 1 1 0-4h.09a1.65 1.65 0 0 0 1.51-1 1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33h0a1.65 1.65 0 0 0 1-1.51V3a2 2 0 1 1 4 0v.09a1.65 1.65 0 0 0 1 1.51h0a1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82v0a1.65 1.65 0 0 0 1.51 1H21a2 2 0 1 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1Z"
  })),
  alert: React.createElement(React.Fragment, null, React.createElement("path", {
    d: "M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"
  }), React.createElement("line", {
    x1: "12",
    y1: "9",
    x2: "12",
    y2: "13"
  }), React.createElement("line", {
    x1: "12",
    y1: "17",
    x2: "12.01",
    y2: "17"
  })),
  check: React.createElement(React.Fragment, null, React.createElement("polyline", {
    points: "20 6 9 17 4 12"
  })),
  clock: React.createElement(React.Fragment, null, React.createElement("circle", {
    cx: "12",
    cy: "12",
    r: "10"
  }), React.createElement("polyline", {
    points: "12 6 12 12 16 14"
  })),
  calendar: React.createElement(React.Fragment, null, React.createElement("rect", {
    x: "3",
    y: "4",
    width: "18",
    height: "18",
    rx: "2",
    ry: "2"
  }), React.createElement("line", {
    x1: "16",
    y1: "2",
    x2: "16",
    y2: "6"
  }), React.createElement("line", {
    x1: "8",
    y1: "2",
    x2: "8",
    y2: "6"
  }), React.createElement("line", {
    x1: "3",
    y1: "10",
    x2: "21",
    y2: "10"
  })),
  link: React.createElement(React.Fragment, null, React.createElement("path", {
    d: "M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"
  }), React.createElement("path", {
    d: "M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"
  })),
  file: React.createElement(React.Fragment, null, React.createElement("path", {
    d: "M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"
  }), React.createElement("polyline", {
    points: "14 2 14 8 20 8"
  })),
  zap: React.createElement(React.Fragment, null, React.createElement("polygon", {
    points: "13 2 3 14 12 14 11 22 21 10 12 10 13 2"
  })),
  play: React.createElement(React.Fragment, null, React.createElement("polygon", {
    points: "6 4 20 12 6 20 6 4"
  })),
  send: React.createElement(React.Fragment, null, React.createElement("line", {
    x1: "22",
    y1: "2",
    x2: "11",
    y2: "13"
  }), React.createElement("polygon", {
    points: "22 2 15 22 11 13 2 9 22 2"
  })),
  rerun: React.createElement(React.Fragment, null, React.createElement("polyline", {
    points: "23 4 23 10 17 10"
  }), React.createElement("path", {
    d: "M20.49 15a9 9 0 1 1-2.12-9.36L23 10"
  })),
  filter: React.createElement(React.Fragment, null, React.createElement("polygon", {
    points: "22 3 2 3 10 12.46 10 19 14 21 14 12.46 22 3"
  })),
  download: React.createElement(React.Fragment, null, React.createElement("path", {
    d: "M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"
  }), React.createElement("polyline", {
    points: "7 10 12 15 17 10"
  }), React.createElement("line", {
    x1: "12",
    y1: "15",
    x2: "12",
    y2: "3"
  })),
  more: React.createElement(React.Fragment, null, React.createElement("circle", {
    cx: "12",
    cy: "12",
    r: "1"
  }), React.createElement("circle", {
    cx: "19",
    cy: "12",
    r: "1"
  }), React.createElement("circle", {
    cx: "5",
    cy: "12",
    r: "1"
  })),
  chevron: React.createElement(React.Fragment, null, React.createElement("polyline", {
    points: "9 18 15 12 9 6"
  })),
  arrow: React.createElement(React.Fragment, null, React.createElement("line", {
    x1: "5",
    y1: "12",
    x2: "19",
    y2: "12"
  }), React.createElement("polyline", {
    points: "12 5 19 12 12 19"
  })),
  x: React.createElement(React.Fragment, null, React.createElement("line", {
    x1: "18",
    y1: "6",
    x2: "6",
    y2: "18"
  }), React.createElement("line", {
    x1: "6",
    y1: "6",
    x2: "18",
    y2: "18"
  })),
  block: React.createElement(React.Fragment, null, React.createElement("circle", {
    cx: "12",
    cy: "12",
    r: "10"
  }), React.createElement("line", {
    x1: "4.93",
    y1: "4.93",
    x2: "19.07",
    y2: "19.07"
  })),
  pencil: React.createElement(React.Fragment, null, React.createElement("path", {
    d: "M17 3a2.85 2.85 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"
  })),
  layers: React.createElement(React.Fragment, null, React.createElement("polygon", {
    points: "12 2 2 7 12 12 22 7 12 2"
  }), React.createElement("polyline", {
    points: "2 17 12 22 22 17"
  }), React.createElement("polyline", {
    points: "2 12 12 17 22 12"
  })),
  pause: React.createElement(React.Fragment, null, React.createElement("rect", {
    x: "6",
    y: "4",
    width: "4",
    height: "16"
  }), React.createElement("rect", {
    x: "14",
    y: "4",
    width: "4",
    height: "16"
  }))
};
function Icon({
  name,
  size = 16,
  ...rest
}) {
  const path = ICONS[name];
  if (!path) return null;
  return React.createElement("svg", {
    className: "icon",
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    "aria-hidden": "true",
    ...rest
  }, path);
}
const MEMBERS = [];
const MEMBERS_BY_ID = Object.fromEntries(MEMBERS.map(m => [m.id, m]));
const TEAMS = ["Operations", "Marketing", "Esport", "GD/VE"];
const TODAY = new Date().toLocaleDateString("en-US", {
  month: "short",
  day: "numeric"
});
const WORK = [];
const WORK_BY_ID = Object.fromEntries(WORK.map(w => [w.id, w]));
const STATUS_LABEL = {
  new: "New",
  need_brief: "Need Brief",
  queued: "Queued",
  assigned: "Assigned",
  in_progress: "In Progress",
  review: "Review",
  delivered: "Delivered",
  blocked: "Blocked",
  cancelled: "Cancelled"
};
const STATUS_CLASS = {
  new: "badge--new",
  need_brief: "badge--need",
  queued: "badge--queued",
  assigned: "badge--assigned",
  in_progress: "badge--progress",
  review: "badge--review",
  delivered: "badge--delivered",
  blocked: "badge--blocked",
  cancelled: "badge--cancelled"
};
const ASSET_LABEL = {
  "static-graphic": "Static",
  "general-video": "General video",
  "motion": "Motion",
  "esport-video": "Esport video",
  "hybrid": "Hybrid"
};
function Avatar({
  memberId,
  size = ""
}) {
  if (!memberId) return React.createElement("span", {
    className: `avatar avatar--unassigned ${size}`
  }, React.createElement(Icon, {
    name: "users",
    size: 11
  }));
  const m = MEMBERS_BY_ID[memberId];
  if (!m) return React.createElement("span", {
    className: `avatar avatar--unassigned ${size}`
  }, React.createElement(Icon, {
    name: "users",
    size: 11
  }));
  return React.createElement("span", {
    className: `avatar ${size}`,
    style: {
      background: m.color
    }
  }, m.initials);
}
function flowmatePrettifyToken(token) {
  if (token == null || token === "") return "";
  return String(token).replace(/[_-]+/g, " ").replace(/\b\w/g, c => c.toUpperCase());
}
function StatusBadge({
  status
}) {
  const label = STATUS_LABEL[status] || flowmatePrettifyToken(status) || "—";
  const cls = STATUS_CLASS[status] || "badge--new";
  return React.createElement("span", {
    className: `badge ${cls}`
  }, label);
}
function PriorityBadge({
  level
}) {
  const label = {
    urgent: "Urgent",
    high: "High",
    normal: "Normal",
    low: "Low"
  }[level] || flowmatePrettifyToken(level) || "—";
  return React.createElement("span", {
    className: `prio prio--${level || "normal"}`
  }, React.createElement("span", {
    className: "prio__mark"
  }), label);
}
function DueBadge({
  delta,
  label,
  status
}) {
  if (delta == null) {
    return React.createElement("span", {
      className: "mono muted"
    }, label || "—");
  }
  if (delta < 0) {
    return React.createElement("span", {
      className: "badge badge--overdue"
    }, label, " · ", Math.abs(delta), "d late");
  }
  if (delta <= 2) {
    return React.createElement("span", {
      className: "badge badge--soon"
    }, label, " · ", delta === 0 ? "today" : `${delta}d`);
  }
  return React.createElement("span", {
    className: "mono muted"
  }, label);
}
function flowmateCsvCell(value) {
  let s = value == null ? "" : String(value);
  if (/^[=+\-@\t\r]/.test(s)) s = "'" + s;
  return `"${s.replace(/"/g, '""')}"`;
}
function flowmateDownloadCsv(filename, headerLabels, dataRows) {
  const csv = [headerLabels, ...dataRows].map(row => row.map(value => flowmateCsvCell(value)).join(",")).join("\n");
  const blob = new Blob([csv], {
    type: "text/csv;charset=utf-8"
  });
  const url = URL.createObjectURL(blob);
  const link = document.createElement("a");
  link.href = url;
  link.download = filename;
  document.body.appendChild(link);
  link.click();
  link.remove();
  URL.revokeObjectURL(url);
}
function flowmateWorkbookXmlText(value) {
  return String(value == null ? "" : value).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
}
function flowmateWorkbookSheetName(name, fallback) {
  const cleaned = String(name || fallback || "Sheet").replace(/[\[\]\*\/\\\?:]/g, " ").replace(/\s+/g, " ").trim().slice(0, 31);
  return cleaned || "Sheet";
}
function flowmateZipLe16(value) {
  return [value & 255, value >>> 8 & 255];
}
function flowmateZipLe32(value) {
  return [value & 255, value >>> 8 & 255, value >>> 16 & 255, value >>> 24 & 255];
}
const FLOWMATE_CRC32_TABLE = (() => {
  const table = [];
  for (let n = 0; n < 256; n += 1) {
    let c = n;
    for (let k = 0; k < 8; k += 1) {
      c = c & 1 ? 0xEDB88320 ^ c >>> 1 : c >>> 1;
    }
    table[n] = c >>> 0;
  }
  return table;
})();
function flowmateCrc32(bytes) {
  let crc = 0xFFFFFFFF;
  for (let index = 0; index < bytes.length; index += 1) {
    crc = FLOWMATE_CRC32_TABLE[(crc ^ bytes[index]) & 255] ^ crc >>> 8;
  }
  return (crc ^ 0xFFFFFFFF) >>> 0;
}
function flowmateZipBytes(parts) {
  const total = parts.reduce((sum, part) => sum + part.length, 0);
  const out = new Uint8Array(total);
  let offset = 0;
  parts.forEach(part => {
    out.set(part, offset);
    offset += part.length;
  });
  return out;
}
function flowmateCreateZipBlob(files, mimeType) {
  const encoder = new TextEncoder();
  const localParts = [];
  const centralParts = [];
  const entries = [];
  let localOffset = 0;
  (files || []).forEach(file => {
    const pathBytes = encoder.encode(file.path);
    const dataBytes = typeof file.content === "string" ? encoder.encode(file.content) : file.content;
    const crc = flowmateCrc32(dataBytes);
    const localHeader = Uint8Array.from([...flowmateZipLe32(0x04034B50), ...flowmateZipLe16(20), ...flowmateZipLe16(0), ...flowmateZipLe16(0), ...flowmateZipLe16(0), ...flowmateZipLe16(0), ...flowmateZipLe32(crc), ...flowmateZipLe32(dataBytes.length), ...flowmateZipLe32(dataBytes.length), ...flowmateZipLe16(pathBytes.length), ...flowmateZipLe16(0)]);
    localParts.push(localHeader, pathBytes, dataBytes);
    entries.push({
      pathBytes,
      crc,
      size: dataBytes.length,
      offset: localOffset
    });
    localOffset += localHeader.length + pathBytes.length + dataBytes.length;
  });
  const centralOffset = localOffset;
  entries.forEach(entry => {
    const centralHeader = Uint8Array.from([...flowmateZipLe32(0x02014B50), ...flowmateZipLe16(20), ...flowmateZipLe16(20), ...flowmateZipLe16(0), ...flowmateZipLe16(0), ...flowmateZipLe16(0), ...flowmateZipLe16(0), ...flowmateZipLe32(entry.crc), ...flowmateZipLe32(entry.size), ...flowmateZipLe32(entry.size), ...flowmateZipLe16(entry.pathBytes.length), ...flowmateZipLe16(0), ...flowmateZipLe16(0), ...flowmateZipLe16(0), ...flowmateZipLe16(0), ...flowmateZipLe32(0), ...flowmateZipLe32(entry.offset)]);
    centralParts.push(centralHeader, entry.pathBytes);
  });
  const centralBytes = flowmateZipBytes(centralParts);
  const endRecord = Uint8Array.from([...flowmateZipLe32(0x06054B50), ...flowmateZipLe16(0), ...flowmateZipLe16(0), ...flowmateZipLe16(entries.length), ...flowmateZipLe16(entries.length), ...flowmateZipLe32(centralBytes.length), ...flowmateZipLe32(centralOffset), ...flowmateZipLe16(0)]);
  return new Blob([flowmateZipBytes([...localParts, centralBytes, endRecord])], {
    type: mimeType
  });
}
function flowmateXlsxCellRef(columnIndex, rowIndex) {
  let column = "";
  let n = columnIndex + 1;
  while (n > 0) {
    const remainder = (n - 1) % 26;
    column = String.fromCharCode(65 + remainder) + column;
    n = Math.floor((n - 1) / 26);
  }
  return `${column}${rowIndex + 1}`;
}
function flowmateXlsxWorksheetXml(sheet) {
  const rows = sheet.rows.length ? sheet.rows : [["No rows"]];
  const rowXml = rows.map((row, rowIndex) => `<row r="${rowIndex + 1}">${(row || []).map((cell, columnIndex) => `<c r="${flowmateXlsxCellRef(columnIndex, rowIndex)}" t="inlineStr"><is><t xml:space="preserve">${flowmateWorkbookXmlText(cell)}</t></is></c>`).join("")}</row>`).join("");
  return ['<?xml version="1.0" encoding="UTF-8" standalone="yes"?>', '<worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main">', "<sheetData>", rowXml, "</sheetData>", "</worksheet>"].join("");
}
function flowmateDownloadWorkbook(filename, sheets) {
  const safeSheets = (sheets || []).filter(sheet => sheet && sheet.name && Array.isArray(sheet.rows));
  if (!safeSheets.length) return;
  const workbookSheets = safeSheets.map((sheet, index) => `<sheet name="${flowmateWorkbookXmlText(flowmateWorkbookSheetName(sheet.name, `Sheet ${index + 1}`))}" sheetId="${index + 1}" r:id="rId${index + 1}"/>`).join("");
  const workbookRels = safeSheets.map((sheet, index) => `<Relationship Id="rId${index + 1}" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/worksheet" Target="worksheets/sheet${index + 1}.xml"/>`).join("");
  const sheetContentTypes = safeSheets.map((sheet, index) => `<Override PartName="/xl/worksheets/sheet${index + 1}.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml"/>`).join("");
  const files = [{
    path: "[Content_Types].xml",
    content: ['<?xml version="1.0" encoding="UTF-8" standalone="yes"?>', '<Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types">', '<Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/>', '<Default Extension="xml" ContentType="application/xml"/>', '<Override PartName="/xl/workbook.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml"/>', '<Override PartName="/xl/styles.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.styles+xml"/>', sheetContentTypes, "</Types>"].join("")
  }, {
    path: "_rels/.rels",
    content: ['<?xml version="1.0" encoding="UTF-8" standalone="yes"?>', '<Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">', '<Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument" Target="xl/workbook.xml"/>', "</Relationships>"].join("")
  }, {
    path: "xl/workbook.xml",
    content: ['<?xml version="1.0" encoding="UTF-8" standalone="yes"?>', '<workbook xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships">', "<sheets>", workbookSheets, "</sheets>", "</workbook>"].join("")
  }, {
    path: "xl/_rels/workbook.xml.rels",
    content: ['<?xml version="1.0" encoding="UTF-8" standalone="yes"?>', '<Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">', workbookRels, `<Relationship Id="rId${safeSheets.length + 1}" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/styles" Target="styles.xml"/>`, "</Relationships>"].join("")
  }, {
    path: "xl/styles.xml",
    content: ['<?xml version="1.0" encoding="UTF-8" standalone="yes"?>', '<styleSheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main">', '<fonts count="1"><font><sz val="11"/><name val="Calibri"/></font></fonts>', '<fills count="1"><fill><patternFill patternType="none"/></fill></fills>', '<borders count="1"><border/></borders>', '<cellStyleXfs count="1"><xf numFmtId="0" fontId="0" fillId="0" borderId="0"/></cellStyleXfs>', '<cellXfs count="1"><xf numFmtId="0" fontId="0" fillId="0" borderId="0" xfId="0"/></cellXfs>', '<cellStyles count="1"><cellStyle name="Normal" xfId="0" builtinId="0"/></cellStyles>', "</styleSheet>"].join("")
  }];
  safeSheets.forEach((sheet, index) => {
    files.push({
      path: `xl/worksheets/sheet${index + 1}.xml`,
      content: flowmateXlsxWorksheetXml(sheet)
    });
  });
  const blob = flowmateCreateZipBlob(files, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet");
  const url = URL.createObjectURL(blob);
  const link = document.createElement("a");
  link.href = url;
  link.download = filename.endsWith(".xlsx") ? filename : `${filename}.xlsx`;
  document.body.appendChild(link);
  link.click();
  link.remove();
  URL.revokeObjectURL(url);
}
function Effort({
  value,
  lg
}) {
  if (value == null) return React.createElement("span", {
    className: "muted mono"
  }, "—");
  return React.createElement("span", {
    className: `effort${lg ? " effort--lg" : ""}`
  }, value);
}
function TypePill({
  type
}) {
  if (type === "quick") return React.createElement("span", {
    className: "tag",
    style: {
      background: "#FFF7E6",
      color: "#8A4A12"
    }
  }, "Quick");
  return React.createElement("span", {
    className: "tag",
    style: {
      background: "#E4ECF2",
      color: "#2E546D"
    }
  }, "Creative");
}
function Progress({
  done,
  total
}) {
  if (!total) return React.createElement("span", {
    className: "muted mono"
  }, "—");
  const pct = Math.round(done / total * 100);
  return React.createElement("span", {
    className: "row",
    style: {
      gap: 6
    }
  }, React.createElement("span", {
    style: {
      display: "inline-block",
      width: 36,
      height: 4,
      borderRadius: 2,
      background: "var(--garena-light-grey)",
      position: "relative",
      overflow: "hidden"
    }
  }, React.createElement("span", {
    style: {
      position: "absolute",
      inset: `0 ${100 - pct}% 0 0`,
      background: "var(--garena-deep-blue)"
    }
  })), React.createElement("span", {
    className: "muted mono",
    style: {
      fontSize: 11
    }
  }, done, "/", total));
}
function Source({
  children
}) {
  return React.createElement("div", {
    className: "src"
  }, "Source: ", children);
}
Object.assign(window, {
  Icon,
  ICONS,
  MEMBERS,
  MEMBERS_BY_ID,
  TEAMS,
  TODAY,
  WORK,
  WORK_BY_ID,
  STATUS_LABEL,
  STATUS_CLASS,
  ASSET_LABEL,
  Avatar,
  StatusBadge,
  PriorityBadge,
  DueBadge,
  Effort,
  TypePill,
  Progress,
  Source,
  flowmatePrettifyToken,
  flowmateCsvCell,
  flowmateDownloadCsv,
  flowmateDownloadWorkbook
});
